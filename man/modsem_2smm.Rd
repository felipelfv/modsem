% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modsem_2smm.R
\name{modsem_2smm}
\alias{modsem_2smm}
\title{Two-Stage Method of Moments (2SMM) for Latent Interactions}
\usage{
modsem_2smm(
  model.syntax,
  data,
  center.data = FALSE,
  standardize.data = FALSE,
  se.type = "sandwich",
  error.dist = "general",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{model.syntax}{lavaan-style model syntax}

\item{data}{A data frame with observed variables}

\item{center.data}{Center observed variables before estimation?}

\item{standardize.data}{Standardize observed variables before estimation?}

\item{se.type}{Type of standard errors: `"sandwich"` (default, HC0)}

\item{error.dist}{Distribution assumption for measurement errors:
`"general"` (default, distribution-free) or `"normal"` (assumes normal errors)}

\item{verbose}{Print progress?}

\item{...}{Additional arguments passed to `lavaan::cfa()` in Stage 1}
}
\value{
An object of class `c("modsem_2smm", "modsem")`
}
\description{
`modsem_2smm()` estimates latent variable interaction models using the
Two-Stage Method of Moments approach (Wall & Amemiya 2000, 2003).

Stage 1 fits a CFA via lavaan and computes Bartlett factor scores.
Stage 2 performs bias-corrected method-of-moments regression on the
factor scores to estimate structural parameters including interaction effects.
}
\examples{
\dontrun{
library(modsem)

m1 <- "
  X =~ x1 + x2 + x3
  Z =~ z1 + z2 + z3
  Y =~ y1 + y2 + y3
  Y ~ X + Z + X:Z
"

est <- modsem_2smm(m1, data = oneInt)
summary(est)
}

}
\references{
Wall, M. M. & Amemiya, Y. (2000). Estimation for polynomial structural
equation models. *Journal of the American Statistical Association*, 95(451),
929-940.

Wall, M. M. & Amemiya, Y. (2003). A method of moments technique for fitting
interaction effects in structural equation models. *British Journal of
Mathematical and Statistical Psychology*, 56(1), 47-63.
}
